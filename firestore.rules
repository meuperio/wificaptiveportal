rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only the backend system service account can access the database.
    // Client SDK is not used in the frontend. All traffic routes through Express.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
