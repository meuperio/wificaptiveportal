rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allows public access. In a production app, use Firebase Admin SDK with a service account.
    // For this AI Studio preview, the backend Express server acts as the client SDK.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
